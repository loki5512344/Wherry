# Maintainer: loki5512344 <dev14dbq@gmail.com>
# Contributor: Your name <your@email.com>

pkgname=wherry
pkgver=0.1.0
pkgrel=1
pkgdesc="A modern dual-pane file manager with SFTP/FTP/FTPS support"
arch=('x86_64')
url='https://github.com/loki5512344/Wherry'
license=('GPL3')
depends=(
  'webkit2gtk-4.1'
  'gtk3'
  'libsoup3'
  'librsvg'
  'libappindicator-gtk3'
  'dbus'
  'openssl'
)
makedepends=(
  'cargo'
  'rust'
  'patchelf'
)
source=("https://github.com/loki5512344/Wherry/archive/refs/tags/v$pkgver.tar.gz")
sha256sums=('SKIP')

_repodir="Wherry-$pkgver"

prepare() {
  cd "$srcdir/$_repodir/src"
}

build() {
  cd "$srcdir/$_repodir/src"
  export CARGO_TARGET_DIR="$srcdir/target"
  cargo build --release
}

package() {
  cd "$srcdir/$_repodir/src"
  install -Dm755 target/release/wherry "$pkgdir/usr/bin/wherry"
  install -Dm644 icons/128x128.png "$pkgdir/usr/share/pixmaps/wherry.png"
  install -Dm644 "$srcdir/$_repodir/dist/wherry.desktop" "$pkgdir/usr/share/applications/wherry.desktop"
}
