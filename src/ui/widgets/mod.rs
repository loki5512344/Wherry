//! Переиспользуемые UI-примитивы: кнопки с обратной связью, кликабельные строки,
//! модальные подложки. Вынесены сюда, чтобы отклик на действия был единообразным
//! во всём интерфейсе (см. правило про мгновенный визуальный отклик) и без копипасты.
pub mod button;
pub mod context_menu;
pub mod modal;
pub mod overlay;
pub mod row;
